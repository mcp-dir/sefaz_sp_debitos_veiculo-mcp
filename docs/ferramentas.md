# Ferramentas

SEFAZ SP: Débitos de Veículo expõe 1 ferramenta (todas somente leitura).

### 1. `sefaz_sp_debitos_veiculo_consultar`
**Input**: `placa`, `renavam`, `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional)

SEFAZ SP: Débitos de Veículo, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar SEFAZ SP: Débitos de Veículo
```
